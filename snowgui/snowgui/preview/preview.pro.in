#
# preview.pro.in -- qt configuration file for the preview subappliction
#
# (c) 2016 Prof Dr Andreas Mueller, Hochschule Rapperswil
#

QT       += core gui widgets

TEMPLATE = lib
CONFIG += staticlib

# Directories for ICE and the astrophotography library
ICEDIR = @ICEDIR@
ASTROPATH = @ASTROPATH@

LIBS += -L$${ASTROPATH}/lib -lastro -liceastro
LIBS +=	-L$${ICEDIR}/lib/c++11 -Wl,-rpath,$${ICEDIR}/lib/c++11 -lIce -lIceUtil

INCLUDEPATH += $${ASTROPATH}/include
INCLUDEPATH += $${ICEDIR}/include
INCLUDEPATH += ../common

FORMS    +=								\
	previewwindow.ui

SOURCES += 								\
	previewwindow.cpp 						\
	PreviewImageSink.cpp

HEADERS  += 								\
	previewwindow.h							\
	PreviewImageSink.h				

